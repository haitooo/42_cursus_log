#include "vbc.h"

int	main(int argc, char **argv)
{
	if (argc != 2)
		return (1);
	int	i = 0;
	char	*str = argv[1];
	int	length = ft_strlen(str);
	while (i < length)
	{
		if (str[i] == '(')
		{
			
		}
	}
}
